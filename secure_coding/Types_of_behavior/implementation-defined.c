#include <stdio.h>

int main() {
    int x = -1;
    unsigned int y = 1;

    if (x < y) {
        printf("x is less than y\n");
    } else {
        printf("x is not less than y\n");
    }

    return 0;
}